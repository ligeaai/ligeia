import React from "react";
import Main from "../../../layout/main/main";

const AssetReporting = () => {
  return <div>reporting</div>;
};

const reporting = () => {
  return <Main Element={AssetReporting()} delSearchBar={true} />;
};

export default reporting;
