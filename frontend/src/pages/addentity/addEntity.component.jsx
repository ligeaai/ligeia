import {Grid} from "@mui/material";
import React from "react";
import {gridSpacing} from "../../store/constant";

export default function AddEntity() {
  return (
    <Grid container={gridSpacing} style={{margin: 20}}>
      Hello
    </Grid>
  );
}
